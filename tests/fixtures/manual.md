# Product A 3 — operator note

同步节点在检查点完成后更新。

Set `EXAMPLE_MODE` in `/opt/example/config.ini`.

Product A is 40% faster than Product B.

## Product B — version unspecified

The relay fabric coordinates the group.
